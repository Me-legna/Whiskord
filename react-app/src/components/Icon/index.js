//set up to be resuable component to use for serverIcon, privateServerIcon userMessageIcon, memberIcon, createServerIcon
function Icon({
    iconImage, //image to render
    isServer=false //give css class for hover effect if True
}){
    return (
        <></>
    )
}

export default Icon
